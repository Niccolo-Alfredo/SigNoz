---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart signoz-otel-collector
  ansible.builtin.systemd:
    name: signoz-otel-collector.service
    state: restarted